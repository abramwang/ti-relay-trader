# 债享5号拒单质量核对

核对日期：`2026-08-04`

业务时区：`Asia/Shanghai`

账户：`314000046830`，别名“债享5号”

## 结论

绩效页面查询 `2026-07-01..2026-08-04` 时显示的 25 个“订单与成交账本异常”，并非 25 个订单/成交数据故障，而是 `trade_quality.v2` 将每个 `status=rejected` 委托无条件计入异常造成的口径误报。

25 笔委托全部发生在 `2026-07-06 09:30:04.366..09:30:12.418 CST`，均为普通股票卖出，终态和柜台终态均为 `rejected`，`is_terminal=true`，成交回报数量与本地成交账本数量均为 0，且全部返回 `BROKER_REJECTED / VIP:找不到持仓`。它们是有完整原因的业务拒单，应保留在拒单数和拒单率中，不应阻断或警告订单与成交账本质量。

区间内其他质量指标为：10,130 笔委托、9,233 笔有成交委托、9,665 条去重成交、0 笔未终态、0 个孤立成交组、0 个订单/成交数量差异。券商交割单已经逐笔证明有成交订单的证券、方向、数量、金额、时间和费用闭合；人工净值表也确认 7 月 6 日经济结果，不存在这批零成交拒单带来的盈亏或费用影响。

## 权威证据边界

券商交割单 `reference/314000046830_20260804.csv` 是成交和结算资金的权威证据，但正常不会包含未成交的拒单/废单。因此它可以证明这 25 笔委托没有产生成交和费用，不能单独证明每一条拒单回报的委托状态字段。

原始 OC 归档中 `sk.W.1141069.0 / 002049.SZ` 和 `sk.W.1141191.0 / 688187.SH` 各有两条相邻拒单回报，`order_stream_id` 均为空。前者两次回报的 `invalid_vol/leaves_qty` 分别为 `200/0` 和 `100/100`；后者 `invalid_vol` 分别为 200 和 300。稳定订单的拒单终态、证券、方向、委托量、零成交和拒绝原因不受影响，因此当前不修改历史账本，也不影响绩效。

若后续需要对这两组旧协议原始回报做字段级裁决，需要券商导出 `2026-07-06` 的“当日委托/废单流水”，至少包含：客户号、委托编号/合同号、证券代码、买卖方向、委托数量、成交数量、撤单或废单数量、委托状态、废单原因、委托时间。普通交割单和人工净值表无法替代该证据。这个补充材料不是当前绩效计算或账本质量放行的前置条件。

## 修正

`trade_quality.v3` 将业务结果与数据质量分开：

1. `rejected_orders` 和 `reject_rate` 继续统计所有拒单。
2. `rejected_orders_with_reason` / `rejected_orders_missing_reason` 明确展示拒单证据覆盖。
3. 有 `reject_message` 或 OC 柜台原因的拒单不进入 `anomalies`。
4. 拒单缺少原因时返回 `rejected_order_missing_reason`；未终态、数量差、上下文错配、终态冲突和孤立成交等原质量门禁保持不变。
5. 本次只修正计算和展示口径，没有更新或删除生产订单、订单事件、成交、费用及原始 Redis 归档。
